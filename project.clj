(defproject com.palletops/pallet-test-env-parent "0.1.8-SNAPSHOT"
  :packaging "pom"
  :aliases {"jar" ["do"] ; do is a no-op when called with no arguments
            "install" ["do"]
            "deploy" ["do"]})
